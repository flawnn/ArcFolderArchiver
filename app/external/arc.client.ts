class ArcClient {
  async extractFolderData(url: string): Promise<object | null> {
    // TODO: Implement logic to fetch Arc URL and extract JSON
    return null;
  }
}

export const arcClient = new ArcClient();
